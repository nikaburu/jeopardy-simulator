<UserControl x:Class="OwnGame.Views.QuestionProcessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Converters="clr-namespace:OwnGame.Converters" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding QuestionProcess, Source={StaticResource Locator}}">
	<UserControl.Resources>
		<Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
	</UserControl.Resources>
    <StackPanel>
        <DockPanel>
            <Button Content="Back" DockPanel.Dock="Left" Width="60" Command="{Binding UnLoadQuestionCommand}" />
            <Button Content="Show answer" Width="100" DockPanel.Dock="Right" HorizontalAlignment="Right" Command="{Binding MakeAnsweredCommand}" />
        </DockPanel>        
    	<TextBlock Text="{Binding Model.Text}" />
        <StackPanel Visibility="{Binding IsAnswered, Converter={StaticResource ResourceKey=BoolToVisibilityConverter}}">
            <Border>
                <TextBlock Text="{Binding Model.Answer}" />
            </Border>            
        </StackPanel>
    </StackPanel>
</UserControl>
